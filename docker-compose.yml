version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: docker/php/Dockerfile
    container_name: kickpredict_app
    working_dir: /var/www
    volumes:
      - .:/var/www
    expose:
      - "9000"
    ports:
      - "5173:5173"
    depends_on:
      - db
    environment:
      - DB_CONNECTION=pgsql
      - DB_HOST=db
      - DB_PORT=5432
      - DB_DATABASE=kickpredict
      - DB_USERNAME=kickpredict_user
      - DB_PASSWORD=kickpredict_password

  db:
    image: postgres:15
    container_name: kickpredict_db
    restart: unless-stopped
    environment:
      POSTGRES_DB: kickpredict
      POSTGRES_USER: kickpredict_user
      POSTGRES_PASSWORD: kickpredict_password
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"

  webserver:
    image: nginx:alpine
    container_name: kickpredict_webserver
    ports:
      - "8000:80"
    volumes:
      - .:/var/www
      - ./docker/nginx/default.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - app

volumes:
  postgres_data: 